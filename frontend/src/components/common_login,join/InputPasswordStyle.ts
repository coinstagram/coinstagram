import styled from 'styled-components';

const StyledPassword = styled.div`
  white-space: nowrap;
  margin: auto;
  .toggleBtn {
    font-weight: bold;
    font-size: 1rem;
    outline: none;
  }
`;

export default StyledPassword;
