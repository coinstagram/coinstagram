import React from 'react';
import styled from 'styled-components';

const StyledDiv = styled.div`
  height: 60px;
  padding: 16px;
`;

function PostListHeader() {
  return <StyledDiv>PostListHeader</StyledDiv>;
}

export default PostListHeader;
