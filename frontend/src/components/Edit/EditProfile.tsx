import React from 'react';
import Thumbnail from '../common/Thumbnail';

function EditProfile() {
  return (
    <div>
      <Thumbnail size={50} imageUrl={null} />
    </div>
  );
}

export default EditProfile;
